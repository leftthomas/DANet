_BASE_: "base.yaml"
MODEL:
  WEIGHTS: "detectron2://ImageNetPretrained/MSRA/R-50.pkl"
  SEM_SEG_HEAD:
    NUM_CLASSES: 21
  DILATED_RESNET:
    DEPTH: 50
DATASETS:
  TRAIN: ("voc_2012_train",)
  TEST: ("voc_2012_val",)
SOLVER:
  IMS_PER_BATCH: 16
OUTPUT_DIR: "./output/r50/voc"
